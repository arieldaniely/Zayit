[package]
name = "zayita-installer"
version = "1.0.0"
edition = "2021"
description = "Silent installer for Zayita application"
authors = ["KDroidFilter"]

[dependencies]
# Windows API bindings
windows = { version = "0.58", features = [
    "Win32_Foundation",
    "Win32_UI_WindowsAndMessaging",
    "Win32_UI_HiDpi",
    "Win32_Graphics_Gdi",
    "Win32_System_LibraryLoader",
    "Win32_System_Threading",
    "Win32_Security",
] }

# Image decoding and resizing
image = { version = "0.25", default-features = false, features = ["png"] }

# Windows registry access (for detecting old MSI installations)
winreg = "0.52"

[build-dependencies]
embed-resource = "2.4"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true

[[bin]]
name = "zayita-installer"
path = "src/main.rs"
